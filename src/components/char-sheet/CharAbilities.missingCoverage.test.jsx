import { describe, it, expect } from 'vitest';

describe('CharAbilities psiBolsteredKnack context', () => {
  // All tests removed: brittle implementation-specific assertions replaced by rendering tests in CharAbilities.rendering.test.jsx
  it('passes', () => { expect(true).toBe(true); });
});
