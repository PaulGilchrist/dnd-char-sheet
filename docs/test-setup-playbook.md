## PITFALLS CHECKLIST (master list — subagents MUST run through this before declaring incomplete)

1. **Premature "setup gap"** — never claim a character can't cast a spell without reading `public/data/2024/spells.json` / `public/data/spells.json` to verify class access first.
2. **Wrong spell class attribution** — verify spell class lists from JSON before claiming a class can't use a spell; check BOTH `/data/` and `/data/2024/` paths.
3. **Server caching blame** — `changeData.js` is in-memory + 10s debounce. If edits don't appear, wait 15s+, reload, or Admin → Clear Change Data. Real issue is usually the client failing to POST.
4. **Wrong target type** — add the correct NPC via Encounter Builder rather than declaring the test blocked.
5. **Level requirements** — 6th-level spells need lv13+ characters; check slot levels before casting.
6. **`.sp-overlay` intercepts clicks** — a lingering spell/save modal overlay blocks pointer events; dismiss it or use `evaluate el.click()` before the next click.
7. **EB monsters join initiative only via "Join Encounter"** — the select checkbox alone never reaches combatSummary.
8. **GM features are localhost-only** — test via http://localhost (port 80) or Vite dev (also localhost); network clients are read-only.
9. **One shared SSE connection** — never `new EventSource` directly; use `subscribeToSSE`.
10. **Damage popups stack** — dismiss `click to dismiss` popup between attacks or clicks hit the overlay.
11. **Numbered pitfall refs (21/24/28/29/33/40/41) in older entries below** are pre-truncation references from a lost master list; interpret each from its inline context in the registry/entry it appears in.
12. **Once-per-turn te-buffs only re-arm at the HOLDER's own turn** — `turnStartEffects.js` clears used-flags only for `activeName`; a benefit consumed *by* the target from *others'* attacks silently degrades to once-per-round. Probe with a 2nd attacker mid-round before trusting "once per turn" wording (SP-099).
13. **Monster-card `.mc-overlay` persists across avatar clicks** — a stale monster modal intercepts the next click and fires the wrong attacker's action; close it via its × before opening the next card.
14. **Initiative-card "Add" condition modal**: after picking a condition chip you MUST press **Apply** — and its `.ea-overlay` silently blocks ALL subsequent avatar clicks until closed (CLA-295).
15. **EB monster attacks from Prone roll `mode:normal`** — attacker-side prone disadvantage is NOT modeled for EB monsters; for adv/dis triggers use Reckless Attack self-advantage (DraconicDragon) instead.
16. **Ancients Paladin is Frightened-immune (Aura of Courage)** — self-inflicted Frightened auto-removes on next damage before modals evaluate; use Charmed/Deafened for condition-cure tests (CLA-296).
17. **Initiative-card Add-modal is SINGLE-select** — multiple chips + one Apply persists only the LAST chip (CLA-296).
18. **EB attack needs popup "Done"** (`dice-roll-reroll-btn`) click to apply damage — dismissing by background click silently abandons the hit; avatar `img.avatar-image.click()` opens `.mc-overlay` (wrapper click no-ops).
19. **Generic `reaction_damage` branch (CLA-297) had NO reaction tracker/once-gate** — repeat clicks re-fire and `lastAttack` self-overwrites let the holder target SELF; EB ranged attacks persisted `weaponType:'melee'` in lastAttack so range gates silently fail. FIXED 2026-09-04 (see CLA-297 recipe below): gates keyed to trigger `damage_from_adjacent_creature` only — Guardian's `creature_disengages_or_hits_other_within_5ft` shares the same generic branch and must NOT get the holder-target gate.
20. **Special Actions rows:** click the `b.clickable` label via `evaluate el.click()` — clicking the description span no-ops (CLA-298).
21. **Two same-tick `setRuntimeValue` POSTs race:** each POSTs the FULL store snapshot; earlier POST can land last server-side, silently reverting the later key (CLA-298 G2) — decisive evidence is POST-body order in the network inspector.
22. **Admin → Clear Change Data ZEROES Superiority Dice** (getSuperiorityDice fallback 4 while sheet max stays class-table) — Short Rest to refill before any maneuver row works (MN-017).
23. **VERDICT POLICY (orchestrator):** unenforced trigger/gates ("row live anytime", no reaction spend, fires on successes) = FAIL even if damage math is exact — subagents must not PASS-subset an ungated trigger (MN-017 conversion precedent).
24. **Ritual Adept auditing trap:** `automation.ritualSpells.length>0` shows "collected" but the sole consumer no-ops the name; real enforcement is the hardcoded `isWizard` gate (CharSpells.jsx:406).
25. **Sheet computed `prepared` state** is re-overlaid client-side (CharSheet.jsx:122) — fresh in-page getPlayerStats shows all-Prepared even when sheet is un-prepared; trust sheet DOM + change-data.
26. **Feats with `benefits` but `automation:null` are display-only text** — only ASI observability; spell/utility benefits have zero consumers (FT-068).
27. **`rulesFactory.getPlayerStats` in-page probe** needs FULL arrays (classes,equipment,magicItems,races,spells,playerSummary) + merged /data+/data/2024 spell lists, and import `.default`.
28. **Wizard-step tab buttons** have concatenated accessible names ("8 Feats") — locate tabs by innerText match + evaluate click, not getByRole name.
29. **3-score ASI feats** surface 'any'-choice combobox on Abilities step: `page.selectOption('.bg-ability-select >> nth=N', ...)` nth by feat order (featBuffService.js:202).

30. **Initiative "Next →" walks creatures-array order, not init-sorted** — top-level change-data `activeCreatureName` is truth for turn ownership; combatSummary mirror can be stale (CLA-309).
31. **`.sp-overlay` save prompts queue BEHIND result popups** — press Done before Roll Save becomes clickable (CLA-309).
32. **Un-armed `resolveTarget` in teleport/riders yields phantom "Unknown" creatures** in te/state/log — arm target first (CLA-309 Improved Shadow Step).
33. **Auto-assigned spell checkboxes (wizard step 14) are display-only** for non-caster subclass spell grants — never persist to `spells[]` (CLA-308).
34. **`mi-overlay` re-opens on every Edit-wizard save** when `magicInitiateInstances:[]` — `.mi-skip` first (CLA-308).
35. **Any spellless character shows `spell_slots_level_1..9:0`** seeded globally — never slot evidence (CLA-308).

36. **Post-roll reaction rows (Shadowy Dodge) re-fire infinitely on one lastAttack** incl. repeat rollback-heal to max (CLA-310).
37. **Party-member combatSummary currentHp/maxHp can read 1/1 placeholders** — initiative-card spinbuttons are HP truth (CLA-310).
### `[object Object]` ghost campaign — root cause + guard (2026-09-04)
Symptom: `public/campaigns/[object Object]/` respawns after deletion. Root cause: a browser POST of a FULL-store snapshot (incl. old `lastAttack`) with `campaignName` as an OBJECT (encodeURIComponent({}) → `%5Bobject%20Object%5D`) — original payload dated Sep 1 (HeroesFeastBard vs Archmage 1, CLA-249-era run). It then PERSISTS because `changeData.readFile()` ingests every campaign dir at startup and `saveFile()` mkdirs+rewrites every in-memory key per 2s debounce — deleting the folder while any server runs resurrects it. PROVEN not unit tests: full suite (30954 pass) with server stopped did NOT recreate it. GUARD added: `changeData.js` readFile skips corrupted dirs + saveFile refuses/coerced-invalid keys purges them from memory. Removal procedure: stop server FIRST, then `rm -rf`. If it ever returns again, capture the POST in devtools Network to find the client passing an object campaignName (suspect storage.set/setRuntimeValue callers).

### FT-067/FT-068 feat-ASI recipe (2026-09-04)
Feats step: tick feat via `.list-item-checkbox-trigger` → Abilities step `.bg-ability-select` (nth per feat order) pick ability → Save. Verify ASI applied exactly once in disk base+bg+feat math + featAbilityChoices key `"<Feat>-<n>"`.

## RECIPES

### Orchestrator playbook rules (append-only; subagents MUST NOT rewrite existing entries)
- Edit the playbook with surgical appends only — full-file rewrites have silently dropped prior entries twice.
- Manifest edits: surgical text edits to the single row's block only; never `json.dump` the whole file (unescapes \u across unrelated lines). Set `verified` to exactly `"verified"` when fixed — no custom prose in that field.

### Phantom-row house standard (CLA-284, 2026-09-04)
Phantom/misattributed row = manifest data bug, not code: fix the row IN PLACE (keep the id), retype to `feat`/`featBenefit`, add CANONICAL/misattributed annotation to expectedBehavior with real router/handler paths (cited feat/classFeature handler paths are systematically fictitious — verify against automationRouter.js/automation/index.js). `verified:"verified"` may rest on the handler's passing vitest suite when no live character holds the prerequisite (state the basis). Widen the owning feat row's expectedBehavior to cover the full benefit set.

### CLA-297 Retaliation reaction gates FIXED (2026-09-04 — `.opencode/plans/bug-CLA-297-retaliation-no-reaction-or-range-gates.md`)

- **FIXED:** `reactionDamageHandler.js` generic no-save branch gained `gateAdjacentDamageReaction()` for trigger `damage_from_adjacent_creature` ONLY (Guardian `creature_disengages_or_hits_other_within_5ft` + Reactive Strike polearm share the branch — un-gated by design). Gates in order: no lastAttack → holder-not-target → self-attacker (`attackerName === playerStats.name`, kills stale self-overwrite re-fire) → `totalDamage <= 0` (findLastAttack computes `actualDamage ?? primary+secondary`) → `attackEvent.weaponType === 'ranged'` → `isWithinRange(holder, attacker, 5)` (lenient no-map=true) → round latch `_Retaliation_usedRound` vs `getCombatContext(cs).round` (CLA-274/CLA-298 interception consumption precedent). Refusals = `automation_info` popups (CharReactions renders via `setPopupHtml(result.payload)`), no roll, no stamp, no log. On pass: stamp round + `ability_use` "X used Retaliation (Reaction) — melee attack against Y…" then `attack_roll`. Round-wrap clears added to navigationHandlers.js + initiative.jsx next-turn lists.
- **FIXED (collateral, upstream weaponType):** `MonsterCardModal.jsx` computed `isMelee = attackRange <= 5` but never forwarded it into `rollAttack` opts → `attackPostProcessing.js` fell back to `damageType === 'ranged'` → EB Heavy Crossbow lastAttack `weaponType:'melee'`. Adding `isMelee` to the opts fixes every downstream weaponType consumer. EB monster attack `range` field is description-only text (`"100/400 ft."` — no `range`/`reach` key on ranged monster actions; only melee actions carry `reach`), so reach-derived `isMelee` is the truth, and `rangeToFeet('100/400 ft.')` returns null — never parse it.
- **LIVE:** round 1 Thug Mace HIT 3 dmg → Retaliation offered ✓ HIT(21 vs 11) 8 dmg + ability_use log + latch=1; re-click → refusal popup "you cannot attack yourself" (stale self-overwritten lastAttack; fresh same-round enemy trigger → round-spend refusal, vitest-locked); crossbow HIT 7 (change-data `weaponType:'ranged'` now correct) → "last attack was a Ranged attack" refusal, latch unconsumed; Next-wrap (14 clicks) to round 2 → re-offered ✓ HIT(25 vs 11) 15 dmg, latch=2, 2nd ability_use log.
- **VITEST:** 9 gate cases in `reactionDamageHandler.test.js` (pass-through+log+stamp, same-round refuse, next-round re-arm, holder-not-target, self, zero-damage, ranged, map out-of-range, no-attack). Full suite 30977 pass, lint zero.

### CLA-295 Restore Balance armed-flag reaction FIXED (2026-09-04 — `.opencode/plans/bug-cla-295-restore-balance-no-prompt-always-on-passive.md`)

- **FIXED:** cross-player adv/dis cancels in this app follow the **Cosmic Omen pending-flag precedent (CLA-065)**, NOT a prompt modal: Reactions row click → `restoreBalanceHandler.js` spends `restorebalanceUses` + arms `restoreBalanceArmed` = `JSON.stringify({armedAt})` (sequential awaits — pitfall 21); refusal popups for no-uses and already-armed (no double-spend). Consumption at the single `logAndShow` seam (`useLoggedDiceRollAttack.js`, covers PC sheet AND MonsterCardModal rolls — all rollAttack/rollSavingThrow funnel through it): if `ctx.forcedMode` is adv/dis, `consumeArmedRestoreBalance` (`src/services/combat/restoreBalanceState.js`) enumerates armed holders (roller + combatSummary creatures), gates Blinded + `isWithinRange(holder, roller, 60)` (lenient no-map=true), clears flag, logs `ability_use` "…used Restore Balance (Reaction): X's attack roll is made without Advantage or Disadvantage.", forces `forcedMode:'normal'` (propagates to popup + roll log via ctx).
- **FIXED (core defect):** passive channel deleted — `collectSaveModifiers` no longer emits `effect:'restore_balance'`, `saveModifierApplies` hard-rejects it (`return false`), `applySaveModifiers` branches removed → `conditionEffects.restoreBalance` can never be set → holder-side `getNetAttackMode`/`hasSaveAdvantage`/`makeSaveContext`/`handlePlayerSaveDamage` cancel consumers are inert (kept, inert-flag harmless; pure-function tests untouched). `'restoreBalanceArmed'` added to LONG_REST restRules-constants list.
- **PITFALL:** `computeD20Roll` has no async seam — any async gate (isWithinRange, map loads) must live in `logAndShow` BEFORE computeD20Roll; the roller's `context.name` can arrive undefined on Skip-modal re-render flows, so reaction logs must phrase `rollName ? rollName+' ' : ''` fallback or print "undefined".
- **PITFALL (tests):** suites that `getRuntimeValue.mockReturnValue(N)` globally will poison armed-flag reads — make `isRestoreBalanceArmed` require a parsed `{armedAt}` OBJECT (raw `'3'`/3 → not armed); key-specific `mockRuntime` maps must use the exported KEY constant, not a shorthand prop name.
- **VITEST:** `restoreBalance.test.js`/`automationModifiers.test.js`/`conditionEffects.test.js`/`conditionEffectsCompute.test.js` producer blocks flipped to "never collected/set"; new `restoreBalanceState.test.js` (arm/gate/consume/self-roller) + `useLoggedDiceRollAttack.restore-balance.test.js` (seam: cancel adv→normal, no-armed keeps adv, normal rolls never consult) + handler arming block. Lint zero, full suite 30968 pass.
- **LIVE:** LR → row click → change-data `{restorebalanceUses:0, restoreBalanceArmed:'{"armedAt":…}'}`; Dragon Reckless adv roll → log `[11,13] mode:normal` + consume ability_use + flag null; NEXT adv roll `[4,12] advantage` (single cancel).

### Heroic Inspiration / Resourceful long-rest grant (CLA-294, 2026-09-04)
Inspiration state = per-character `hasInspiration` BOOLEAN in change-data + Summary checkbox on sheet (`CharSummary.jsx:283`). Spend = uncheck the box (no roll-consumption UI needed). Grant gate: `restRules-longRest.js:148` checks `playerStats.specialActions` contains "Resourceful" (display categorization, NOT the `passives` categorization from automationRouter.js:669 — two parallel categorizations; trait-name match is the real gate). Cap is boolean 1 max (re-rest while unspent re-writes true, no stacking — acceptable). Human-gated: Orc control (AberrantSorcerer) long-rests with NO hasInspiration key.

### 2024 Resistance cantrip two-stage cast (SP-099, 2026-09-04)
Edit-wizard Spells step: dismiss lingering `.mi-overlay` via `.mi-skip` first → tick spell via `.list-item-checkbox-trigger` (row click unreliable) → ✓ Save INSIDE `.character-creation-wizard-overlay`. Cast: Spells cell → popup "Cast Spell" → `.sp-overlay` SecondaryTargetModal (`.secondary-target-row`) → "Cast Resistance" → SingleResistanceSelectionModal radio + `.sp-roll-btn`. RAW once-per-turn probe: Join a 2nd monster (auto-active at lower init) and attack in the same round before the target's own turn — popup/hp_change delta is decisive. EB Thug ×2 = proven Bludgeoning/Piercing attacker pair.

### WM-004 Push weaponMastery auto-apply FIXED (2026-09-04 — `.opencode/plans/bug-wm-004-push-mastery-no-consumer.md`)

- **FIXED:** `MASTERY_EFFECTS` gained `Push {effect:'push', value:10, sizeLimit:'large_or_smaller'}` (weaponMasteryHandler.js) — base-mastery Push now auto-applies post-hit like Sap/Slow via buildTacticalMasterStep (no save, no picker — the Mastery popup cell stays text-only by design for base masteries; picker only for choice/replace features). Push branch writes instant te `{target, source:'Push', option:'Push', effect:'push', value:10, duration:'instant'}` (shieldBash write shape, log-only push precedent MN-015 — no grid position math) + ability_use log "…used Push on X — pushed 10 feet straight away."; Huge refusal logs "attacked X with a Push weapon but did not push it — Push: Target is Huge (too large — only Large or smaller affected)." with NO te and size gate reuses MN-015 `validateSizeLimit` (import from class-fighter-rogue/executeManeuver.js — monster data first, combatSummary target.size fallback). `push` registered in targetEffectDefinitions.js (Movement/'Pushed'/fa-hand).
- **FIXED (collateral):** buildTacticalMasterStep auto-apply loop used `campaignName` as characterKey for `_X_appliedTarget` flags → 4 runtime-guard console errors + flag landed on fake 'test-campaign' creature bucket; now `getRuntimeValue('campaign', key, campaignName)` (flag surfaces as TOP-LEVEL change-data key, same bucket as targetEffects).
- **PITFALL (kind gate):** `collectWeaponMastery` nulls baseMastery unless `weapon_kind_mastery` passive matches AND runtime `_Weapon_Kind_Mastery_chosenWeapons` contains the weapon (automationPassives.js `!hasKindMasteryMatch → baseMastery=null`). **Admin → Clear Change Data wipes chosen weapons** and every mastery auto-apply silently stops while the sheet Mastery cell still shows "Push" (display-only getWeaponMastery reads equipment un-gated). Re-arm: sheet Special Actions "Weapon Mastery:" row → kind modal → tick Warhammer → Select ("Weapon kinds set to: Warhammer." popup).
- **PITFALL:** per-target once-applied flags `_Push_appliedTarget` etc. mean a 2nd hit on the SAME target writes no te/log — for repeat-push evidence use a fresh target or clear change-data between hits. Damage popups stack per click; always dismiss `click to dismiss` popup before next attack.
- **LIVE EVIDENCE (lv20 Barbarian Warhammer, pre-fix baseline zero-delta):** Medium EB Thug 1 HIT → te `{target:'Thug 1', source:'Push', effect:'push', value:10, duration:'instant'}` + ability_use log; control hit #3 (flag armed) added NO te/log; EB Hill Giant 1 (Huge) HIT → refusal log, NO push te. Registry stays Warhammer-equipped.

### MN-015 Pushing Attack save DC + EB monster size gate FIXED (2026-09-04 — `.opencode/plans/bug-mn-015-push-attack-save-dc-and-size-gate.md`)

- **FIXED (pitfall 21 retired):** (1) DC — `useAttackDamageResolution.handleAttackRiderManeuverUse` now forwards the combat_superiority info from `playerStats.automation.specialActions/actions` (info-builder already resolves numeric `saveDc = 8 + best(STR,DEX) + PB`) into the rider action, falling back to `saveDc:"ability", saveAbility:["STR","DEX"]` (buildSaveDc "ability" branch takes ability[0]); live `.sp-overlay` "Saving Throw Required DC 14" + `save_result` log "succeeded STR save (DC 14...)", `[buildSaveDc] no saveDc` console error gone. (2) Size gate — supplier-side: `encounterToInitiative.js` (both constructors) and `initiativeService.applyNpcMonsterData` now write `size` (and it lands in change-data `creatures[].size`, e.g. Gibbering Mouther 1 Medium / Hill Giant 1 Huge); `validateSizeLimit` resolves `getMonsterData(targetName, null)?.size || target.size || "Medium"` (monster data first, suffix-strip in getMonsterData). Huge target → refusal BEFORE the die roll (old roll-then-refund block deleted — refusal now never expends nor free-writes the pool), returns `{refused:true}` + ability_use refusal log; the hook skips the damageBonus die-add on `result.refused` so base weapon damage still resumes clean. 
- **PITFALL:** the size-refusal popup is rendered but immediately covered by the base weapon-damage popup (refusal then `resumeAttackPipeline()` applies base damage) — the DECISIVE refusal evidence is the `ability_use` log "Pushing Attack: Target is Huge (too large — only Large or smaller affected)." + pool unchanged + damage popup with NO "+N" rider die + no STR save prompt. 
- **PITFALL:** rider modal "Use Maneuver" `.sp-roll-btn` is DISABLED until the maneuver radio is ticked; the row label is behind the sheet grid so trusted clicks report the char-special-actions div intercepting — `evaluate el.click()` on the radio then the enabled button works (React synthetic events fire via element.click()). 
- **VITEST:** vitest throws "No X export is defined on the mock" when a MISSING factory export is first ACCESSED (call-time), not at import link — so adding a static `getMonsterData` import into `executeManeuver.js` only broke suites that actually CALL it (combatSuperiorityChoices — fixed by adding `loadMonsters: vi.fn(async () => [])` to its dataLoader factory; suite still passes, its fixtures have no sizeLimit-bearing maneuvers). 
- **TESTS:** new `executeAttackRider.mn015.test.js` (DC 14 via real buildSaveDc partial-mock + Huge refusal no-die/no-save), `validateSizeLimit.mn015.test.js` (monster-data-first resolution, target.size fallback, no-sizeLimit short-circuit), `useAttackDamageResolution.mn015SaveDc.test.js` (numeric DC forwarding, "ability" fallback, refusal adds no die + logs). Lint zero, full suite 30925 pass. 

### MN-016 Rally row→modal dispatch FIXED (2026-09-04 — `.opencode/plans/bug-mn-016-rally-bonus-row-no-modal-dispatch.md`)

- **FIXED (pitfall 29 pattern confirmed):** `rallyChoice: simpleModal('rallyChoiceModal')` added to modalMap (useCharActionsAutomation.js) — Rally Bonus-Actions row `b.clickable` now opens the SecondaryTargetModal picker (`.sp-overlay` "Rally", targets = `{label,value}` allyOptions — SecondaryTargetModal natively supports option-shape targets via `'value' in target`); `handleRallyChoiceConfirm` now flushes `result.logEntries` (canonical "Rally: X gains N temporary hit points." grant log + d10Roll reaches campaign log); `executeBonusActionManeuver` rolls/expend AFTER the allies-empty gate (no-allies click: no roll, no spend, no Relentless-latch disarm).
- **PITFALL:** change-data keys are NESTED (`GET /change-data` returns `{combatSummary, EvasiveFighter:{...}}`) — probe `d['EvasiveFighter'].BattleMasterManeuvers_selection`, not flat `EvasiveFighter.key`; `rally_clear` expiration lands under the SOURCE (`EvasiveFighter.pendingExpirations`, not the ally's).
- **PITFALL:** combat-log endpoint is `/api/campaigns/:campaign/log` (not `/api/log/...`).
- **PITFALL:** after Confirm Selection the Bonus-Actions rows for newly selected maneuvers may not render until a full page reload (`navigate /`, re-enter campaign + char).
- **PITFALL (vitest):** `evaluateAutoExpression` is consumed TWICE in the bonus path — `superiority_die` (die size) and `fighter_level / 2` (extra HP) — a constant-return mock makes rollExpression get '1d9'; make the mock expression-aware (`expression === 'fighter_level / 2' ? 9 : 12`).
- **LIVE:** first click round 1 = Relentless free d8 ("Rolled d8 for 5 (Relentless)", pool 6 unchanged, `relentlessUsedRound` stamped); 2nd click same round = paid d12 (pool 6→5); tempHp grant = die + 9 (lv18), higher value wins on re-grant (14 → 15).
- **TESTS:** new `executeActionManeuvers.rally.test.js` (modal shape + math + spend, Relentless free, no-allies no-roll/no-spend/no-latch), rallyChoice dispatch test in `useCharActionsAutomation.test.class-feature-modals.js`, logEntries flush + no-logEntries tests in `useCharActionsModalHandlers.combatSuperiority.test.js`. Lint zero, full suite 30931 pass.

### SP-097 Ray of Enfeeblement (2024) fallback DC 10 / Unknown target / no repeat save FIXED (2026-09-04 — `.opencode/plans/bug-sp-097-ray-of-enfeeblement-dc-target-repeat-save.md`)

- **FIXED:** (1) DC — `rayOfEnfeeblementService.js` now forwards numeric `saveDc` (`metaCtx.spellSaveDc || playerStats.spellAbilities.saveDc` — holdMonsterService shape) into `automation`; live prompt "Thug 1 must make a CON saving throw DC 17" (was Unknown/DC 10). (2) Target — `CharSpells.jsx` Spells-table flow now passes the real CharActions-sibling `getTargetInfo` (getCombatContext → getTargetFromAttacker → getAttackerTargetName) instead of `async () => null`; te lands `target:"Thug 1"` with `dc:17` + `activeConditionMeta {ray_of_enfeeble_debuff:{dc:17,ability:'con'}}`. (3) Repeat save — Enfeeblement badge onClick → `onRollConditionSave(creature, {key:'ray_of_enfeeble_debuff',...,ability:'con'})` (Otto/Maze shape); `createRollConditionSaveHandler` gained a ray success branch (clear te + caster concentration + save_result + "save ends" condition-removed log) and a failed-branch "kept ... continues" log. Handler also now `storage.set('combatSummary')` + dispatches `combat-summary-updated` after addConcentration (Confusion pattern) — without it the concentration record never persisted.
- **PITFALL:** `spellPreparationService.js` `addConcentration(cs, playerName, spell.name, 10, targetName)` hardcodes DC 10 for EVERY concentration spell on cast — it persists BEFORE the spell handler resolves; the handler's later storage.set is what rescues the DC. Any spell that mutates concentration but skips storage.set shows stale dc 10.
- **PITFALL:** long-running dev session + HMR = ghost save prompts: an unresolved pre-fix prompt stays in persisted `pendingSavePrompts`; after reload SavePromptModal re-presents it, `.sp-overlay` intercepts clicks (pitfall 33) and Roll Save can resolve the GHOST prompt while the live handler promise never resolves (no save_result log, te never written). Detect via `GET /change-data` `pendingSavePrompts`/`pendingSaveListenerPrompts` ids; fix = Admin → Clear Change Data, re-Join Encounter, re-arm target.
- **PITFALL:** EB monster card in Initiative does NOT open a details modal from avatar/card click — repeat-save badge clicks render a `[data-testid="popup-overlay"]` condition-save popup that intercepts the NEXT badge click; dismiss it (or `evaluate el.click()` on the badge) between attempts. Badge-click repeat save result shows in `.condition-save-result` ("SAVE FAILED/SUCCESSFUL (DC 17)"), NOT `.sp-overlay`.
- **PITFALL:** monsters join initiative ONLY via Encounter Builder "Join Encounter" button (appears when ≥1 monster selected); "Select Thug" checkbox alone never reaches combatSummary.
- **PITFALL:** `target-select` dropdowns are ordered by creature-card order (joined monster card is index 0) — locate the caster index by card textContent, never hardcode nth.
- **NOTE:** 2024 wizard `spells[]` on disk is bare names; castable/prepared state comes from computed playerStats — the Spells-table row renders clickable when prepared.
- **MODEL:** success-branch (`disadvantage_next_attack` te + 1-round expiration, no concentration record written by handler — spellPreparation's dc-10 record stands) untouched, per house model; badge-click remains the app-wide end-of-turn repeat-save consumer (SP-066/CLA-175 family).
- **TESTS:** new `CharSpells.getTargetInfo.sp-sp097.test.jsx` (executor seam: real function resolves armed target/overlay-name/null); new cases in `rayOfEnfeeblementService.test.js` (saveDc threading from metaCtx + spellAbilities fallback), `rayOfEnfeeblementHandler.test.js` (DC 17 prompt, te carries dc, activeConditionMeta merge/null, concentration persisted/skipped), `createRollConditionSaveHandler.test.jsx` (ray cleanup: te+conc cleared + save-ends logs, unrelated caster conc preserved, failed keeps te + "continues" log, no-effect no-op), `ConditionEffectBadges.spell-effects.test.jsx` (badge click → con repeat save with te dc). Lint zero, scoped suites (features/ handlers/ char-sheet/ initiative/) 15310 pass.

## Pitfall: attack damage formula string is pre-built before featureRiders mutate damageType

Sheet attack pipelines build the damage formula (e.g. `1d8+5 [slashing]`) in
`buildRollBaseDamageStep` *before* `buildFeatureRidersStep` runs. A rider that swaps
`ctx.attack.damageType` (e.g. Sacred Weapon radiant) correctly changes the applied/logged
damage type (`rollDamage` ctx reads `attack.damageType` via `contextBuilder-sync`), but the
already-built formula string in the popup still shows the old `[type]` token. Verify radiant
conversion via the campaign log's `damageType` field, not the formula text.

## Pitfall: Sacred Weapon CD display lags modal-open

`sacredWeaponHandler.handle` spends the Channel Divinity charge *before* returning the
damage-type picker modal, so the sheet's "X/3" counter can briefly show the pre-spend value
while the picker is open, and the post-spend value is only correct after the next SSE sync.
Cancel refunds via `cancelSacredWeapon`; verify spends/refunds against
`GET /api/campaigns/test-campaign/change-data` (10s debounce), not the instant UI counter.
